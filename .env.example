# MongoDB URI (dapatkan dari MongoDB Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/wa-chatbot?retryWrites=true&w=majority

# PORT untuk HTTP server (diisi otomatis oleh Render)
PORT=10000
